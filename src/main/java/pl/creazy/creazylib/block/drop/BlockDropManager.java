package pl.creazy.creazylib.block.drop;

import org.bukkit.block.Block;
import org.bukkit.enchantments.Enchantment;
import org.bukkit.entity.Player;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;
import pl.creazy.creazylib.drop.BonusDropSupport;
import pl.creazy.creazylib.drop.DropManagerBase;
import pl.creazy.creazylib.id.Id;
import pl.creazy.creazylib.part.constraints.Part;

//@Part
public class BlockDropManager extends DropManagerBase<BlockDrop> {
//  public @Nullable BlockDrop getDrop(@Nullable Block block) {
//    var id = Id.get(block);
//    return getDrop(id);
//  }
//
//  public int getFinalDropAmount(@Nullable Player player, @NotNull BlockDrop drop) {
//    return getFinalDropAmount(player, drop, BonusDropSupport.FORTUNE, Enchantment.FORTUNE);
//  }
}
